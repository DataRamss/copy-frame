# Copy Frame 插件使用说明

## 1. 插件简介

Copy Frame 可以帮你在浏览器中快速高亮并锁定目标区域，一键复制适合发给 AI 的页面定位描述和当前页面链接，便于你准确表达“我要改这里”。

如果你正在用 AI 开发网页、调试前端界面，或者使用 HTML 制作 PPT，Copy Frame 都很适合你。

适合的场景包括：

- 向 AI 描述网页上某个按钮、输入框、卡片、弹窗或区域
- 快速定位页面中的 DOM 元素并复制稳定描述
- 使用 AI 开发网页、调试前端界面或制作 HTML PPT
- 辅助开发、联调、页面改版沟通

---

## 2. 主要能力

插件当前支持：

- 点击 Chrome 工具栏中的插件图标，开启检查模式并显示启动提示；提示右侧的设置图标可打开快捷键设置面板
- 鼠标移动时高亮当前可选中的 DOM 元素
- 点击页面中的元素后锁定选区
- 根据元素的 `data-testid`、`name`、`aria-label`、`id`、类名、文本等信息生成友好的元素名称
- 自动尝试生成更稳定的定位选择器
- 一键复制包含当前页面链接、元素名称和定位选择器的描述文本
- 在锁定后通过键盘在同级、父级、子级元素之间快速切换
- 支持撤销到上一次锁定的选区

---

## 3. 安装

### 3.1 从 Chrome 应用商店安装

在 Chrome 应用商店搜索 `Copy Frame`，或直接打开：

**[前往 Chrome 应用商店安装 Copy Frame](https://chromewebstore.google.com/detail/copy-frame/hcikfcfccmpahpdofmalkakdkinalhgo)**

点击“添加至 Chrome”即可完成安装。安装后建议将 Copy Frame 固定到工具栏。

如果需要在本地 HTML 或 `file://` 页面中使用，请前往扩展详情页开启“允许访问文件网址”，然后刷新页面。这项权限必须由用户手动开启。

### 3.2 开发者本地调试

普通用户不需要下载或构建源码。参与开发时，可以在项目根目录执行：

```bash
npm install
npm run typecheck
npm run build
```

构建完成后，打开 `chrome://extensions/`，启用“开发者模式”，点击“加载已解压的扩展程序”，选择项目下的 `dist` 目录。

---

## 4. 基本使用流程

### 4.1 开启检查模式

点击 Chrome 工具栏中的 Copy Frame 图标后，插件进入检查模式，并在右下角显示启动提示：

- 页面上会出现粉色高亮框
- 需要查看或调整启动快捷键时，点击启动提示右侧的设置图标
- 关闭设置面板后，检查模式仍会继续工作
- 鼠标移动到哪里，就会高亮当前命中的可选 DOM 区域
- 标签上会显示插件推断出的元素名称

### 4.2 锁定目标区域

将鼠标移动到目标区域后，直接点击：

- 当前区域会被锁定
- 右上角会出现 `Copy` 按钮
- 插件进入“已锁定”状态

### 4.3 复制描述文本

锁定后，你可以：

- 点击高亮框右上角的 `Copy` 按钮
- 或使用复制快捷键（见下文）

复制后的内容通常类似：

```text
页面：<https://example.com/login>
[登录页] 登录按钮 :: button[data-testid="login-button"]
```

如果目标元素本身名字比较泛，插件会自动补充父级上下文，帮助 AI 更准确理解目标位置。

---

## 5. 快捷键说明

> 启动快捷键默认为 `Alt + C`，可在设置面板中修改。下面快捷键需要先点击页面元素完成锁定后使用。

| 作用 | 快捷键 | 说明 |
| --- | --- | --- |
| 复制 | `Ctrl + C` / `Cmd + C` | 复制当前锁定区域描述 |
| 撤销 | `Ctrl + Z` / `Cmd + Z` | 回到上一次锁定结果 |
| 同级切换 | `Tab` / `Shift + Tab` | 在兄弟节点之间前后切换 |
| 父子级切换 | `Enter` / `Shift + Enter` | 进入子元素或回到父元素 |
| 退出锁定 | `Esc` | 从“已锁定”回到“检查中” |

### 5.1 关于浏览器级快捷键
当前版本默认使用 `Alt + C` 作为页面级开关快捷键，也可以在设置面板中改成其他组合键。
默认操作方式是：

- 按当前启动快捷键开启或关闭插件
- 或点击 Chrome 工具栏中的 Copy Frame 图标来开启插件；需要设置时点击启动提示右侧的设置图标，再次点击插件图标可关闭插件

---

## 6. 使用建议

为了让复制出来的结果更稳定、更适合发给 AI，建议：

- 尽量优先选择带有 `data-testid`、`name`、`aria-label` 或 `id` 的元素
- 如果第一次点中的区域太大，可以按 `Enter` 进入更细一层
- 如果点中的区域太小或太具体，可以按 `Shift + Enter` 回到父级
- 如果同级有多个相似元素，可以用 `Tab` / `Shift + Tab` 快速切换
- 复制后可直接粘贴到 AI 对话中，并补充你的修改目标或参考说明，例如“请把这里改成圆角按钮”

---

## 7. 当前限制

当前版本的限制包括：

- 主要面向 DOM 元素定位，不适合识别纯画布内容
- `canvas` 区域目前不支持复制稳定命名
- 一些浏览器受限页面、特殊跨域页面，可能无法正常注入或工作
- 生成的描述以“稳定定位 + 友好命名”为主，不等同于完整的视觉截图理解

---

## 8. 推荐使用场景

推荐在这些场景使用：

- 页面改版时，告诉 AI “修改这一块”
- 联调时，快速确认某个交互区域对应的 DOM 节点
- 让产品、测试、开发更容易表达页面目标区域
- 为自动化、测试、AI 辅助开发提供更明确的定位描述

---

## 9. 一句话总结

Copy Frame 的核心用法可以概括为：

**点击插件图标开启 -> 鼠标悬停查看 -> 点击锁定 -> 按 `Copy` 或 `Ctrl/Cmd + C` 复制描述。**
